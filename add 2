#include<stdio.h>
int input(int*x,int*y)
{
	printf("enter two numbers");
	scanf("%d",&*x);
	scanf("%d",&*y);
}
int compute(int a,int b)
{
 	int c;
 	c=a+b;
	return c;
}
void output (int h)
{ 
	int h;
}
